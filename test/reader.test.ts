import { expect } from 'chai';
import { StringStream, LiteralPredicate } from '../src/reader'
import { SyntaxNode } from '../src/ast'


describe('TokenStream tests', function () {
    // it('StringStream tests', function () {
    //     const stream = new StringStream("abcdefg");

    //     //can stream match predicate?
    //     const predicate = new LiteralPredicate("abc")

    //     //YES
    //     const node: SyntaxNode = predicate.apply(stream)
    //     expect(node).to.not.be.null
    //     expect(node.children.length).to.equal(1)
    // });
});